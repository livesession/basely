```ts
import basely from 'basely'

const img = await basely.image("@basely/components", {
    import: "Baseline",
    props: {
        title: "Node.js support",
        toolGroups: [
            [
                {
                    tool: "bun",
                    supported: true,
                }
            ],
            [
                {
                    tool: "pnpm",
                    supported: true,
                }
            ],
             [
                {
                    tool: "npm",
                    supported: true,
                }
            ]
        ]
    }
})

const img = await basely.baseline({
    props: {
        title: "Node.js support",
        toolGroups: [
            [
                {
                    tool: "bun",
                    supported: true,
                }
            ],
            [
                {
                    tool: "pnpm",
                    supported: true,
                }
            ],
             [
                {
                    tool: "npm",
                    supported: true,
                }
            ]
        ]
    }
})

const img = await basely.image("@basely/components", {
    import: "Baseline",
    props: {
        title: "Node.js support",
        toolGroups: [
            [
                {
                    tool: "bun",
                    supported: true,
                }
            ],
            [
                {
                    tool: "pnpm",
                    supported: true,
                }
            ],
             [
                {
                    tool: "npm",
                    supported: true,
                }
            ]
        ]
    }
})

import basely from 'basely'

const data = metrics.last7Days()

const metricsImage = await basely.img("@livesession/design-system", {
    import: "Metrics",
    props: data
})

await slack.post(metricsImage)

```